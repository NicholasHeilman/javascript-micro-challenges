console.log( 'JS 110' );

function conditional( num1, num2 ){
    if ( num1 > num2){
        return ('The First Number was bigger!');
    } else if  
        (num1 < num2) {
        return ('The second number was bigger!');
    } else  if 
        (num1 = num2 ){
        return ( 'They are the same!');
        };
    }

console.log(conditional(4,7));
console.log(conditional(9,1));
console.log(conditional(3,3));